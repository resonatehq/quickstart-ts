aio:
  subsystems:
    queuing:
      config:
        connections:
          - kind: http
            name: summarize
            pattern: /gpu/summarize/*
            metadata:
              properties:
                url: http://localhost:5001